Tuffy (Thatcher Ulrich, thatcherulrich.com) — released into the PUBLIC
DOMAIN by its author. Committed here as the test fixture for hermetic font
resolution tests: a face with a known family name that exists on no OS by
default, so a test-built font database proves the resolver rather than the
machine it runs on.
